{
  "name": "tiny-terminal-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor mobile wrapper for tiny-terminal.",
  "scripts": {
    "sync": "npx cap sync",
    "android": "npx cap run android",
    "ios": "npx cap run ios",
    "build": "echo 'No build step needed; static web in ./../web'",
    "open:android": "npx cap open android",
    "open:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
